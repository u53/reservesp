<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ly.reservesp.dao.FoodInformationMapper">

    <select id="findFoodInfo" resultType="FoodInformation">
        SELECT * FROM FoodInformation INNER JOIN SellingTime ON SellingTime.sellingTimeId = FoodInformation.sellingTimeId INNER JOIN foodType ON foodType.foodTypeId = FoodInformation.foodTypeId;
    </select>


</mapper>
